import { BasicTarget } from '../../common/interface';
declare function useMutationObserver(target: BasicTarget, callback: MutationCallback, config?: MutationObserverInit): readonly [any, any];
export default useMutationObserver;
