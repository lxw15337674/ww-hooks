import { BasicTarget } from '../../common/interface';
export declare type Size = {
    width: number;
    height: number;
};
declare function useSize(target: BasicTarget): any;
export default useSize;
