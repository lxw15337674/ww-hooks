import { BasicTarget } from '../../common/interface';
export declare function useVisible<T extends HTMLElement = HTMLElement>(
  target: BasicTarget<T>,
): boolean | undefined;
export default useVisible;
